
  # Cash Reconciliation System

  This is a code bundle for Cash Reconciliation System. The original project is available at https://www.figma.com/design/H65NdJuwLZQN47EyOo6H5R/Cash-Reconciliation-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  