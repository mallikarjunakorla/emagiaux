/**
 * MainLayout — Backwards-compatibility alias for AppLayout.
 *
 * The router uses AppLayout directly. This file is kept only so that
 * any existing imports of MainLayout continue to compile without errors.
 */

export { AppLayout as MainLayout } from "./AppLayout";
